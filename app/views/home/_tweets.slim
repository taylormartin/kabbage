- unless tweets.nil?
  - tweets.each do |tweet|
    .row
      .col-sm-12
        .tweet-box
          img.profile-img src="#{tweet['pic']}"
          span.user-text=link_to "@#{tweet['user']}", "https://twitter.com/#{tweet['user']}", target: "_blank"
          .tweet-text= tweet['text']
